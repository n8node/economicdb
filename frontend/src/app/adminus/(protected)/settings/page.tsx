export default function AdminSettingsPage() {
  return (
    <div>
      <h1 style={{ marginTop: 0 }}>Системные настройки</h1>
      <p className="muted">
        Следующий этап: OpenRouter API key, Robokassa, SMTP Яндекс — encrypted storage в PostgreSQL.
      </p>
    </div>
  );
}
